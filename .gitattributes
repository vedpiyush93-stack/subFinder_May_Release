artifacts/final_model.pkl filter=lfs diff=lfs merge=lfs -text
artifacts/embeddings_cache/*/fasttext_*_model/*.vectors_ngrams.npy.xz filter=lfs diff=lfs merge=lfs -text
# 5-rep reproducibility suite (one calibrated deploy model per rep, ~173 MB each)
reproducibility/rep_*/final_model.pkl filter=lfs diff=lfs merge=lfs -text
reproducibility/inference/final_model.pkl filter=lfs diff=lfs merge=lfs -text
# Per-rep classifier weights — keras files can be 30-45 MB; joblib are smaller.
# Tracked via regular git (under GitHub's 50 MB per-file warning), but if any
# trial happens to exceed it the LFS rule below catches it.
reproducibility/rep_*/predictions/**/*.keras filter=lfs diff=lfs merge=lfs -text
artifacts/predictions/**/*.keras filter=lfs diff=lfs merge=lfs -text
artifacts/predictions/**/*.joblib filter=lfs diff=lfs merge=lfs -text
reproducibility/rep_*/predictions/**/*.joblib filter=lfs diff=lfs merge=lfs -text
